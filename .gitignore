node_modules/
data/
data/admin-credentials.txt

# NOTE: dist/tailwind.css is COMMITTED (pre-built) so the host can run
# `npm start` with no build step. Rebuild locally with `npm run build`
# whenever you change Tailwind classes, then commit the new CSS.
