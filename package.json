{
  "name": "xteam-website",
  "version": "0.1.0",
  "private": true,
  "description": "Tesla XTeam FX Trade — new website",
  "scripts": {
    "build": "tailwindcss -i input.css -o dist/tailwind.css --minify",
    "dev": "tailwindcss -i input.css -o dist/tailwind.css --watch",
    "start": "node server.js"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
